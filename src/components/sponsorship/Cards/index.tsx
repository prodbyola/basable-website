import './index.scss'
import TierCard from "./Card"

const CardList = () => {
    return (
        <section className="sponsorshipCardList">
            <TierCard />
        </section>
    )
}

export default CardList